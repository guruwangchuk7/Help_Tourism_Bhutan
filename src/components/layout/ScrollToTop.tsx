import { useEffect } from 'react'

const ScrollToTop = () => {
    // Scroll to top is now handled smoothly by PageTransition upon new page mount
    useEffect(() => {}, [])

    return null
}

export default ScrollToTop
